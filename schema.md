---
type: schema
title: 项目目录管理规范
summary: Coding 项目的目录结构、文件写入规范与命名规范。Agent 进入项目时请先读本文档和 START.md。
created: 2026-08-24
updated: 2026-08-24 10:45
---

# 项目目录管理规范

> 本文档是 **Agent 消费指南**，定义本项目的目录结构、文件写入规范与命名规范。不是状态报告。

[[START.md|项目交接文档]]  |  [[preferences|用户偏好]]  |  [[experience|踩坑经验]]

---

## 一、目录结构

**默认骨架**（冷启动创建）：

| 目录                 | 用途          | 示例内容                        | git |
| ------------------ | ----------- | --------------------------- | --- |
| `1-input/rd/`      | 原始需求与设计     | PRD、功能清单、设计稿、接口文档           | 屏蔽  |
| `1-input/notes/`   | 追加需求与计划     | 追加需求、给 Agent 的计划、过程说明       | 屏蔽  |
| `2-agent-room/`    | Agent 专属工作区 | 执行计划、技术方案、会议纪要、杂项、处理脚本（一次性） | 屏蔽  |
| `3-build/private/` | 不希望公开的文档    | 私有文档、配置、记忆文件等               | 屏蔽  |
| `4-test/`          | 测试区         | 测试用例、测试数据、测试脚本（可复用）         | 跟踪  |

> 本项目为工作台单文件应用，**源码文件（index.html / sw.js / manifest / smoke.cjs / 图标 / README 等）直接放根目录**（保持 GitHub Pages 部署结构），治理目录为旁路。勿把源码移入 3-build。

**核心原则**：
- `1-input/` **只进不出**：原始材料（需求/参考）归位，加工后的产物转 `3-build/`，避免变成垃圾场。
- `2-agent-room/` **随便折腾**：Agent 的中间产物/草稿/一次性脚本，不进 git，可清理。
- `3-build/` **正式产出**：进 git；`private/` 放不希望公开的文档（私有配置/记忆/凭证），不可重建、gitignore、不提交。
- `4-test/` **测试资产**：可复用的用例/数据/脚本进 git；一次性验证脚本放 `2-agent-room/`。

---

## 二、文件写入规范

### 2.1 文件自闭环

当用户指定项目目录以外的路径、或上传文件时，Agent 应将其**复制到项目目录对应位置**，确保项目目录内信息完整、可独立迁移。

示例：
- 用户说"参考`{XX路径}`下的 `需求文档.docx`" → 复制到 `1-input/rd/`
- 用户上传了一张设计稿 → 保存到 `1-input/rd/`
- 用户说"把这两个 SKILL 拆开重构" → Plan / Todo / 备份文件放 `2-agent-room/`，重构产出放 `3-build/`

> 如果用户提到的文件属于 Obsidian Wiki（如`_index.md`），不复制，原位读取。将用户提供的 wiki 文件绝对路径更新到`START.md`→ `参考 wiki`

### 2.2 命名规范

- 目录名：英文，小写连字符（如 `2-agent-room`），不用驼峰/下划线
- 文件名：描述性、无歧义。用英文或中文均可，保持一致即可
- 版本文件：如有多个版本，用日期或版本号区分（如 `PRD-v2.md`、`PRD-20260725.md`）

---

## 三、本项目专属约束

- **单文件架构**：不引入打包工具；要拆只能用「无构建 `<script src>` 多文件」。
- **凭证纪律**：PAT / Gist Token 一律放 `3-build/private/`（gitignored），禁止写入公开文件与提交历史。
- **改完必做**：升 `sw.js` 缓存名（`qi-workbench-v1X`）+ 跑 `node smoke.cjs`；需上线时走 Contents API 部署（见 `3-build/private/deploy-scripts/`）。

---

## 四、职责边界

本文件（schema）只定义**目录结构与命名规范**，以下内容不在此处、另行定义：

| 内容                                                                 | 归属                        |
| ------------------------------------------------------------------ | ------------------------- |
| 文档写作格式（frontmatter / Markdown / changelog / README / 发布描述 / issue） | `project-doc-writer`SKILL |
| 发布动作（开源审查 / 打包 / 同步 / 发布）                                          | `project-publish` SKILL   |
| 项目状态 / 执行记录                                                        | START.md                  |
| 用户偏好                                                               | Preferences.md            |
| 踩坑经验                                                               | Experience.md             |
